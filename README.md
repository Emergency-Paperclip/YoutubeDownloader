# YoutubeDownloader
GUI interface for downloading youtube stuff

# Buildiung
If you are on linix you should just be able to run make. In lieu of that you could just pip install pyinstaller and run "pyinstaller --onefile ytdl.py"

Currently you gotta manually pip install everything though
What is everything you ask? Well if you run the python it's whatever it conplains about not having I guess. They're all publicly avalible through pip (probably)

# TODO
Handle bad url

Handle bad file extention/path

Notification when done

Haven't tested makefile on linux yet
